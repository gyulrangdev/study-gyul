# study-gyul

Personal study notes and solved exercises.

## Deep-ML

Deep-ML practice lives in [`deep-ml/`](deep-ml/). For each problem, keep:

- a short problem note
- a NumPy warmup
- a PyTorch attempt
- final submitted code only after solving it yourself

Use `scripts/publish_deep_ml.sh` after updating a problem folder to commit and push the Deep-ML notes.
